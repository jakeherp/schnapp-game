# Create a free database at https://upstash.com, then copy its REST credentials here.
# Without these set, the app falls back to an in-memory store (fine for local
# testing in one process, but games won't sync across real devices).
UPSTASH_REDIS_REST_URL=
UPSTASH_REDIS_REST_TOKEN=
